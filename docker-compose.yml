version: "3"

services:
  postgres:
    build: ./postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    environment:
      HOST: "postgres"

  pgbench:
    build: ./pgbench
    depends_on:
      - pooler
      - postgres

volumes:
  pgdata:
